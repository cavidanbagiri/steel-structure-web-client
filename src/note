

Now, l want to create a main.jsx with extremely beautiful design.
The page will be below form:
1) Pagination will be top and bottom of the table.
2) Filters search box will be bottom of the column names not as seperate component
3) Column must be have asc and desc order ability
4) The first column will have an icon when l click, will open with some options.
5) Column names must be hide and show ability.
6) In thepage must be 2 buttons. 'Import data' and 'Add New'


If you want, you can create a very simple form of the page, if everthing will beautifull, we will go on. Or if you believe you can create a beautifull page, you can create from first time.
Then we will go Transport page.

import React from 'react'

function Main() {
  return (
    <div>Main</div>
  )
}

export default Main
